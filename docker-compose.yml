version: '3.8'

services:
  emmet:
    build:
      dockerfile: Dockerfile.emmet
    command: emmet.exe

  queen:
    build:
      dockerfile: Dockerfile.queen
    command: queen.exe
    depends_on:
      - emmet

